export class TurtleExporter extends Exporter {
    /**
     * Export a given triple list to a file
     * @param {string[][]} tripleList
     */
    exportTriples(tripleList: string[][]): undefined;
}
import { Exporter } from "./exporter";
