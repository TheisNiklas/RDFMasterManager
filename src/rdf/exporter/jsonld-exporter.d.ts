export class JsonldExporter extends StreamExporter {
    /**
     * Export a given triple list to a file
     * @param {string[][]} tripleList
     * @returns {}
     */
    exportTriples(tripleList: string[][]): any;
}
import { StreamExporter } from "./stream-exporter";
